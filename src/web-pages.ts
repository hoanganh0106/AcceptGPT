const escapeHtml = (value: string): string => value.replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char] ?? char));

const pageCss = `:root{--bg:#f4f6fb;--card:#fff;--text:#1a2233;--muted:#5b6474;--accent:#4f46e5;--ok-bg:#e7f6ec;--ok-text:#166534;--err-bg:#fdecec;--err-text:#b91c1c;--warn-bg:#fff4d6;--warn-text:#92400e;--border:#e3e8f0;font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:var(--text);background:var(--bg)}*{box-sizing:border-box}body{margin:0;min-width:320px}.shell{width:min(100% - 2rem,960px);margin:clamp(1rem,6vw,4rem) auto}.card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:clamp(1.25rem,4vw,2rem);margin-bottom:1rem;box-shadow:0 8px 24px rgba(20,30,60,.08)}h1,h2{margin-top:0}p{line-height:1.55}.notice,.message,.result{padding:.85rem 1rem;border-radius:10px}.notice{background:var(--warn-bg);color:var(--warn-text)}.message:empty,.result:empty{display:none}.message-success,.result-success{background:var(--ok-bg);color:var(--ok-text)}.message-error,.result-error{background:var(--err-bg);color:var(--err-text)}.message-warn{background:var(--warn-bg);color:var(--warn-text)}label{display:block;margin:.85rem 0 .35rem;font-weight:700}input,textarea,button{font:inherit}input,textarea{width:100%;border:1px solid var(--border);border-radius:10px;padding:.8rem;background:#fff}textarea{min-height:150px;resize:vertical}input:focus,textarea:focus,button:focus-visible{outline:3px solid rgba(79,70,229,.25);outline-offset:2px;border-color:var(--accent)}.row,.actions{display:flex;gap:.6rem;margin-top:.5rem}.row input{min-width:0}button{border:0;border-radius:10px;padding:.75rem 1rem;background:var(--accent);color:#fff;font-weight:700;cursor:pointer}button.secondary{background:#5b6474}button.danger{background:var(--err-bg);color:var(--err-text)}button:disabled{opacity:.6;cursor:not-allowed}.history-wrap{overflow-x:auto;border:1px solid var(--border);border-radius:10px}table{width:100%;min-width:700px;border-collapse:collapse}th,td{padding:.7rem;text-align:left;border-bottom:1px solid var(--border)}.empty{text-align:center;color:var(--muted)}.badge{display:inline-block;border-radius:999px;padding:.15rem .5rem;font-size:.82rem;font-weight:700}.badge-ok{background:var(--ok-bg);color:var(--ok-text)}.badge-error{background:var(--err-bg);color:var(--err-text)}.badge-warn{background:var(--warn-bg);color:var(--warn-text)}.badge-muted{background:#e9edf4;color:#4d5a70}#created{white-space:pre-wrap;min-height:1.5rem;max-height:240px;overflow:auto;padding:.75rem;border:1px solid var(--border);border-radius:10px;background:#f8f9fc}@media(max-width:620px){.shell{width:min(100% - 1rem,960px)}.row,.actions{flex-direction:column}.row button,.actions button{width:100%}}`;

export function renderRedeemPage(nonce: string): string {
  const safe = escapeHtml(nonce);
  return `<!doctype html><html lang="vi"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Tham gia workspace</title><style nonce="${safe}">${pageCss}.shell{max-width:640px}.result{margin-top:1rem;font-weight:650}</style></head><body><main class="shell"><section class="card"><h1>Tham gia workspace</h1><p class="notice">Session đã xác thực sẽ làm CDK đã dùng vĩnh viễn; nếu bước mời hoặc duyệt sau đó thất bại, CDK không được hoàn lại.</p><form id="redeem"><label for="cdk">CDK</label><input id="cdk" required autocomplete="off" autocapitalize="characters" placeholder="XXXX-XXXX-XXXX-XXXX"><label for="session">ChatGPT session</label><textarea id="session" required autocomplete="off"></textarea><button type="submit">Gửi yêu cầu</button></form><p id="result" class="result" role="status" aria-live="polite"></p></section></main><script nonce="${safe}">(function(){const form=document.getElementById('redeem'),result=document.getElementById('result'),cdk=document.getElementById('cdk'),session=document.getElementById('session'),button=form.querySelector('button');function show(kind,text){result.textContent=text;result.className='result result-'+kind}function busy(on){form.setAttribute('aria-busy',String(on));button.disabled=on;button.textContent=on?'Đang xử lý…':'Gửi yêu cầu'}cdk.addEventListener('input',()=>{cdk.value=cdk.value.toUpperCase()});form.addEventListener('submit',async event=>{event.preventDefault();const sessionValue=session.value;session.value='';busy(true);try{const response=await fetch('/api/redeem',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({cdk:cdk.value,session:sessionValue})});const data=await response.json().catch(()=>({}));if(response.ok&&data.ok){show('success',data.status==='already_member'?'Email '+data.email+' đã là thành viên workspace.':'Đã duyệt thành công cho '+data.email+'. Hãy kiểm tra ChatGPT.');cdk.value=''}else show('error',data.message||'Không thể hoàn tất yêu cầu.')}catch(_){show('error','Không thể kết nối dịch vụ.')}finally{busy(false)}})})();</script></body></html>`;
}

export function renderAdminPage(nonce: string): string {
  const safe = escapeHtml(nonce);
  return `<!doctype html><html lang="vi"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>AcceptGPT Admin</title><style nonce="${safe}">${pageCss}</style></head><body><main class="shell"><header class="card"><h1>Quản trị CDK</h1></header><section id="login" class="card"><h2>Đăng nhập</h2><label for="password">Mật khẩu</label><div class="row"><input id="password" type="password" autocomplete="current-password"><button id="login-button" type="button">Đăng nhập</button></div></section><section id="dashboard" hidden><section class="card"><p class="notice">Hãy bảo đảm Playwright đang ở đúng workspace trước khi phát CDK.</p><label for="workspace">Invite Workspace ID</label><div class="row"><input id="workspace"><button id="save-workspace" type="button">Lưu workspace</button></div></section><section class="card"><p class="notice">Hãy bảo đảm Playwright đang ở đúng workspace trước khi phát CDK.</p><label for="count">Số CDK (1-100)</label><div class="row"><input id="count" type="number" min="1" max="100" value="1"><button id="create-cdks" type="button">Tạo CDK</button></div><pre id="created"></pre><div class="actions"><button id="copy-created" class="secondary" type="button" disabled>Copy tất cả</button><button id="logout" class="secondary" type="button">Đăng xuất</button></div><p>CDK chỉ hiển thị một lần, hãy lưu ngay.</p></section><section class="card"><h2>Lịch sử CDK</h2><div class="actions"><button id="delete-all" class="danger" type="button">Xóa tất cả CDK có thể xóa</button></div><div class="history-wrap"><table><thead><tr><th>Trạng thái</th><th>Email</th><th>Workspace</th><th>Kết quả</th><th>Thời gian</th><th>Thao tác</th></tr></thead><tbody id="history"><tr><td class="empty" colspan="6">Chưa có CDK nào.</td></tr></tbody></table></div></section></section><p id="message" class="message" role="status" aria-live="polite">Đang tải…</p></main><script nonce="${safe}">(function(){let csrf='';const q=id=>document.getElementById(id),message=q('message'),created=q('created'),copy=q('copy-created'),removable=new Set(['join_rejected','accept_not_found','worker_unavailable','upstream_timeout','internal_error','service_interrupted']);function note(text,kind){message.textContent=text;message.className='message message-'+kind}function busy(button,on,label){button.disabled=on;button.textContent=on?'Đang xử lý…':label}async function api(url,options){const init=Object.assign({headers:{}},options||{});if(csrf)init.headers['x-csrf-token']=csrf;const response=await fetch(url,init),data=await response.json().catch(()=>({}));if(!response.ok){const error=new Error(data.message||'Yêu cầu thất bại');error.status=response.status;error.code=data.code;throw error}return data}function badge(value){const el=document.createElement('span'),ok=value==='accepted'||value==='already_member'||value==='used',warn=value==='processing'||value==='join_rejected'||value==='accept_not_found';el.className='badge badge-'+(ok?'ok':warn?'warn':value==='unused'?'muted':'error');el.textContent=value||'—';return el}function canDelete(item){return item.status==='unused'||(item.status==='used'&&removable.has(item.result))}function history(records){const body=q('history');body.textContent='';if(!records.length){const row=document.createElement('tr'),cell=document.createElement('td');cell.className='empty';cell.colSpan=6;cell.textContent='Chưa có CDK nào.';row.appendChild(cell);body.appendChild(row);return}for(const item of records){const row=document.createElement('tr');for(const value of [item.status,item.email||'—',item.workspaceId||'—',item.result||'processing',item.usedAt?new Date(item.usedAt).toLocaleString('vi-VN'):item.createdAt||'—']){const cell=document.createElement('td');if(value===item.status||value===item.result||value==='processing')cell.appendChild(badge(value));else cell.textContent=value;row.appendChild(cell)}const action=document.createElement('td');if(canDelete(item)){const remove=document.createElement('button');remove.className='danger';remove.textContent='Xóa';remove.onclick=async()=>{if(!confirm('Xóa CDK này?'))return;busy(remove,true,'Xóa');try{await api('/api/admin/cdks/delete',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({ids:[item.id]})});await load();note('Đã xóa CDK.','success')}catch(error){note(error.message,'error')}finally{busy(remove,false,'Xóa')}};action.appendChild(remove)}row.appendChild(action);body.appendChild(row)}}async function load(){const data=await api('/api/admin/state');csrf=data.csrfToken;q('workspace').value=data.workspaceId||'';history(data.history.records);q('login').hidden=true;q('dashboard').hidden=false;note('','success')}async function boot(){try{await load()}catch(error){if(error.status===401){q('login').hidden=false;note('','success')}else note('Dịch vụ dữ liệu tạm thời lỗi: '+error.message,'error')}}q('login-button').onclick=async()=>{const button=q('login-button');busy(button,true,'Đăng nhập');try{await api('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:q('password').value})});q('password').value='';try{await load();note('Đăng nhập thành công.','success')}catch(error){q('login').hidden=true;q('dashboard').hidden=false;note('Đăng nhập thành công nhưng chưa tải được dữ liệu — thử tải lại.','error')}}catch(error){note(error.message,'error')}finally{busy(button,false,'Đăng nhập')}};q('save-workspace').onclick=async()=>{const button=q('save-workspace');busy(button,true,'Lưu workspace');try{await api('/api/admin/workspace',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({workspaceId:q('workspace').value})});note('Đã lưu workspace.','success')}catch(error){note(error.message,'error')}finally{busy(button,false,'Lưu workspace')}};q('create-cdks').onclick=async()=>{const button=q('create-cdks');busy(button,true,'Tạo CDK');try{const data=await api('/api/admin/cdks',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({count:Number(q('count').value)})});created.textContent=data.codes.join('\\n');copy.disabled=!created.textContent;await load();note('CDK mới chỉ hiển thị một lần. Hãy sao chép và lưu an toàn.','success')}catch(error){note(error.message,'error')}finally{busy(button,false,'Tạo CDK')}};copy.onclick=async()=>{if(!created.textContent)return;try{await navigator.clipboard.writeText(created.textContent);note('Đã sao chép CDK mới.','success')}catch(_){note('Không thể sao chép tự động.','error')}};q('delete-all').onclick=async()=>{const button=q('delete-all');if(!confirm('Xóa tất cả CDK có thể xóa?'))return;busy(button,true,'Xóa tất cả');try{const data=await api('/api/admin/cdks/delete',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({all:true})});await load();note('Đã xóa '+data.deleted+' CDK.','success')}catch(error){note(error.message,'error')}finally{busy(button,false,'Xóa tất cả CDK có thể xóa')}};q('logout').onclick=async()=>{try{await api('/api/admin/logout',{method:'POST'})}finally{location.reload()}};boot()})();</script></body></html>`;
}
